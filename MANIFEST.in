include README.rst
include LICENSE.txt
include CHANGELOG.rst
include CITATION.cff
include pyproject.toml
include setup.py

exclude .gitignore
exclude .readthedocs.yaml
exclude .yamllint
exclude Makefile
exclude release.txt

# Include the actual package
recursive-include laserbeamsize *.py

# Optional: Include specific test files (if needed)
# recursive-include tests *.py

# Explicitly exclude stuff you don't want
prune docs
prune .github
prune tests
exclude requirements-dev.txt
exclude setup.sh
